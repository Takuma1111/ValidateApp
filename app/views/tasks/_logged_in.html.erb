<h2>タスク一覧</h2>
<table>
    <thead>
      <tr>
        <th>マイページタスク名</th>
      </tr>
    </thead>
    <tbody>
        <% @tasks.each do |task| %>
          <tr>
            <td><%= task.title %></td>

          </tr>
        <% end %>

    </tbody>
</table>
<%= link_to "ログアウト", logout_path, method: :delete %>
